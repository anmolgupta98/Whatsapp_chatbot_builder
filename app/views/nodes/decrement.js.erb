<% add_number_of_nodes = "add_number_of_nodes_" + (@node.id).to_s%>
value = document.getElementById('<%= add_number_of_nodes %>').value;
value = parseInt(value);
if (value > 1){
	document.getElementById('<%= add_number_of_nodes %>').value = value - 1;
}
